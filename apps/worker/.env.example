NODE_ENV=development

# Worker HTTP port (health checks).
PORT=8080

# Redis — BullMQ queue (shared with the API producer).
REDIS_URL=redis://localhost:6379

# Concurrent jobs per worker.
WORKER_CONCURRENCY=5
